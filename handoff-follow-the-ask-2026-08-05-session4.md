# Handoff: Follow the Ask (Toronto development-donor/vote analysis)
Generated 2026-08-05 · Session 4

## Objective
Data journalism piece + interactive site answering: do Toronto councillors who receive money from people in the development industry vote differently on development matters? Methodology has to survive hostile scrutiny. Target publication: mid-to-late September 2026 (Toronto municipal election Oct 26, 2026). Owner: Siri. Repo: `https://github.com/cmd-siri-bot/follow-the-money`, working directory `C:\Users\iamsi\toronto-2026-election`.

**Constraints (unchanged since Session 1):** every external data claim verified live, not assumed. Never put a number on the site that isn't computed from data in the repo. Every classification carries a confidence score, not a binary label. A null/inconclusive result published honestly is a stronger outcome than a strained correlation. Only commit to git when explicitly asked — held throughout this session too; every commit and push this session was on an explicit "commit this"/"push it" instruction.

This session picked up from Session 3's handoff (which had just finished extracting 81 development-applicant identities from staff-report PDFs, not yet integrated into the knowledge graph) and did four things: closed out the 6-PDF-cap open question with a null result, integrated the 81 applicant identities into the knowledge graph (surfacing and fixing a real bug that had been silently undercounting a prior session's headline number), built a second independent lobbying-to-vote linking mechanism from scratch, and used it to answer a new, harder question — "how often do lobbyists get their way" — which the narrower identity-only view had been answering wrong.

## Status

**Done, committed, and pushed to origin/`developer-identity-extraction`** (this branch is not yet merged to `main`; `main` is still at `c851e28`):

- **`c851e28` (already on main, pre-session):** baseline — lobbyist/donor knowledge graph, Progress Toronto donors, motion-text scrape.
- **`914b107`:** developer/applicant identity extraction from staff-report PDFs (Session 3's work, committed at the very start of this session).
- **`2466e8b` — Graph-enhancement name-matching.** Fed the 81 extracted applicant/agent/architect/owner names into the knowledge graph. Built `pipeline/transform/extract_role_name.py` (strips appended mailing addresses from Application Data Sheet free text, classifies org- vs. person-shaped names) and extended `pipeline/transform/build_knowledge_graph.py` with `devapp:` project entities + role edges. Org names auto-merge into existing lobbyist firm/beneficiary entities via the existing `org_key()` mechanism — **53 orgs cross-validated this way** (25 applicant/agent/architect-only, expected professional-firm overlap; **14 owner-only** — the property owner itself, independent of hired consultants, has an existing lobbying relationship — the more novel half). 8 person-name matches, 1 postal-corroborated (Emily Reisman).
  - **Real bug found and fixed while building this, not something the task was looking for:** `link_registrants_to_donors`'s postal-code corroboration check compared raw strings. Lobbyist registrant postal codes are ~76% formatted *with* a space ("M5T 2C8"); `donors.csv` never is. Comparing raw meant most registrants could never be flagged as corroborated even on a genuine address match. Fixed via `normalize_postal()` in `pipeline/transform/normalize_names.py`. **Effect: registrant↔donor postal-corroborated matches went from 39 to 148** (823 total name-matches unchanged); `kg_summary.py`'s same-member money+access overlap metric went from 8 to **33**, now touching 13 of 26 current members instead of 5.
  - New general adjudication worksheet `pipeline/analyze/kg_adjudicate_applicant_overlaps.py` → `audit/kg_corroborated_overlaps_review.csv` (150 rows, supersedes the old hand-picked 8-row file as the complete population — that file is left in place, unchanged, still valid, just no longer representative).
- **`ad6bb26` — LLM first-pass adjudication of the 14 owner-only org matches.** `pipeline/analyze/kg_adjudicate_owner_org_overlaps.py` → `audit/kg_owner_org_overlap_review.csv`. Standouts: **Crestview Investment Corporation** (245 Eglinton Ave E) — 42 same-subject-matter contacts to one councillor (Rachel Chernos Lin) over a full year; **Pinemount Developments Ltd.** — represented in part by Joe Mihevc (the same person flagged in Session 3's registrant-donor adjudication for donating to two mayoral bids), 11 contacts across 11 councillors in a tight May–June 2025 window; **Manulife Ontario Property Portfolio Inc.** — 3 separate registrants each contacted Josh Matlow on the exact same day. **Toronto Community Housing Corporation** and **Parc Downsview Park Inc.** flagged explicitly as public bodies, not private developers — structurally different from the other 12 despite matching the same org-key merge.
- **`93faaca` — Expanded lobbying-to-vote linking via street address; first refusals found.** See "How often do lobbyists get their way" below — this is the session's biggest single piece of work.
- **`3c15dcf` and `f141c8e` — Editorial policy edit + donor geography/political-bloc/map data (user's own work, confirmed, see Corrections & preferences).** `docs/07-editorial-legal.md` had two rules removed ("prefer cluster-level to individual-level presentation," "don't build a reverse lookup by donor name"). New files: `data/processed/donor_geography.json`, `donor_overlap_table.json` (566KB, 3,475 named donors with postal codes and full contribution history), `fsa_boundaries.json`, `lakes.json` (map geometry), `chow_progress_detail.json`, `political_bloc_donors.json`, `political_bloc_fsa.json`, `political_boundaries.json` (donor totals grouped into progressive/conservative/liberal/Gong political blocs, FSA-level). These files were found already sitting in the working tree, not created by this session's own pipeline work — flagged to the user before committing (see Decisions), user confirmed authorship, then committed on explicit instruction.

**Working tree is clean.** Nothing uncommitted. Branch is 1 commit ahead of `origin/developer-identity-extraction` after the last push — actually pushed, confirmed via `git push` — so local and origin should match; re-verify with `git status`/`git log origin/developer-identity-extraction..HEAD` if picking this up fresh.

## Decisions

| Decision | Why | Reopen if |
|---|---|---|
| 6-PDF-per-item cap is not a contributor to the 33.6%/36.0% applicant-extraction coverage gap | Checked all 40 capped items: 11 already found their Data Sheet within the first 6, 28 are policy/study/city-initiated items with no private applicant at all (cap irrelevant), and the 1 genuine candidate (`2023.CC6.13`, 34 real background files) turned out to use a different report template (OLT "Request for Directions" → "Project Data Sheet" with no Applicant/Owner fields at all, only building stats) even after checking the specific file that could have held the answer | Never — this thread is closed. The real explanation (report-template variation) was already named in Session 3 |
| Person-shaped role names matched against the graph's FULL donor/lobbyist_registrant populations, not the narrower `dev_sector_reference.csv` | Follows the same full-recall stance the 2026-08-03 lobbyist/donor deep-dive already established for this graph. `dev_sector_reference.csv` (In-house-only, dev-keyword-only) is a strict subset of what the graph already has, so this loses nothing and gains recall | If/when Phase 4 publication scope needs Signal 3's narrower precision-first framing for this specific population |
| Owner-only org cross-validation notes are structured-context adjudication, not a finished finding | 14 rows read individually; roughly a third show real concentrated activity, a third are thin/empty, two (TCHC, Parc Downsview) are public bodies not private developers | Before treating any of the 14 as a publishable finding — needs the user's own read first |
| For "how often do lobbyists get their way," population locked to development/planning votes only, not all 45,665 votes across every policy topic | User's explicit choice, after being told the all-topics version has no existing linking mechanism at all (procurement/economic-development/etc. lobbying has nothing like the applicant-identity trick) and would be open-ended, low-precision, unbudgetable work | Only if the user explicitly wants to fund/attempt that separate, harder problem later |
| Address-matching (street number + name, normalized) used as a second linking mechanism, confidence-tiered by street-type agreement | User's choice: extend the identity-linked population rather than restrict to it or use generic fuzzy subject-matter-to-title text matching (which this project has avoided before on precision grounds). Address matching is precise (not generic fuzzy text) and needed zero new scraping — checked live, 62% of dev-related lobbyist filings name a street address directly | Never, unless a systematic false-positive pattern is found — the 7 type-mismatched rows were checked individually and all are genuine (street-naming inconsistencies between sources, not different streets) |
| Side classification (developer vs. opposition) via name-based heuristic, explicitly labeled inferred | User's choice, given no stance field exists anywhere in the lobbyist registry. Verified directly, not assumed, that the honest answer for THIS population is "opposition-side lobbying essentially doesn't reach a recorded Council vote" | Never as a general rule — but if a future item is later found to be opposition-side, the classifier needs to actually catch it, not just default to developer |
| Clerk's own "Tracking Status" text used as ground truth for vote disposition, not reconstructed from raw `votes.csv` roll-call joins | A first pass using raw vote joins produced a **wrong, misleading result** (lobbied items looked like they got approved less often) — caught by noticing 9 "Lost" votes all had suspiciously tiny totals (2-3, 1-4), which turned out to be Community-Council-level votes that get forwarded to City Council "without recommendations" on failure, not refused. Confirmed live that 4 of those 9 were later adopted by City Council with amendments | Never — this is a structural fact about Toronto's council procedure, not a one-off data quirk |
| Unexplained working-tree changes (docs/07 edit, donor geography/overlap files) flagged before committing, not committed silently | The specific rule removed ("don't build a reverse lookup by donor name") is exactly what one of the new files (`donor_overlap_table.json`, 3,475 named individuals) does. Per this project's safety standard, flag before acting on anything that looks like it could be a policy change with real stakes | Not applicable — user confirmed authorship same session, resolved |

## Dead ends — don't retry

| Tried | What happened | Why it failed |
|---|---|---|
| Raising the 6-PDF-per-item download cap to recover more Application Data Sheets | Re-derived the full background-file link list live for all 40 capped items via browser page loads (no saved script existed for this from the prior session — a gap now closed by doing it directly rather than re-creating a throwaway script). Found and read the one plausible candidate's actual PDF content | The candidate item's report template doesn't carry an Application Data Sheet at all — a structural population fact, not a coverage/scraping problem |
| Computing lobbying-connected vote outcomes directly from `votes.csv` `(agenda_item_number, vote_description)` joins | Produced a real-looking but **wrong** result (82.4% vs 100%/94% approval, lobbied items looking worse) | Community Council "Lost" ≠ refused in Toronto's procedure — the item just gets forwarded to City Council without a recommendation attached, and Council decides separately, often under a different motion_type ("Amend Item") with no separately-recorded "Adopt Item" roll call at all |

## Corrections & preferences
Carried forward from Sessions 1–3 (still applicable):
- User is not deeply technical with devtools — give explicit step-by-step walkthroughs.
- Always sanity-check file content against filename claims.
- Only commit when explicitly asked; user expects prompt action when they do ask ("commit this", "push it").
- User answers "Recommended" options quickly in `AskUserQuestion` prompts; trusts default judgment calls once the tradeoff is stated clearly.
- User proactively supplies context/corrections mid-task; worth taking at face value and applying immediately, including retroactively to just-completed work if relevant.
- User dismissing/redirecting a question, then saying "keep going," means "use your own judgment," not "ask again."
- User comfortable with rapid direction changes mid-task; pause cleanly, resume smoothly without re-litigating.

New this session:
- **The `/spec` skill, invoked by name, doesn't fit this repo's actual workflow** (no GitHub issues used anywhere in this project's history; `gh` isn't even installed). When invoked, the right move was to flag the mismatch explicitly via `AskUserQuestion` rather than either blindly filing a GitHub issue or silently ignoring the invocation. **The user's first answer picked the full GitHub-issue-filing pipeline, then immediately self-interrupted** ("Do this: rigorous scoping before I touch anything") to redirect to the leaner "scope + execute directly" path once they saw `gh` wasn't installed. Read a self-interruption immediately following an answer as a live correction, not a contradiction to push back on.
- **When the user asks a genuine question mid-`AskUserQuestion`-flow** ("How long and how many tokens will every council vote take?") rather than picking an option, answer the actual question directly and concretely (with real numbers/comparisons where possible) before re-presenting the choice — don't just re-ask the same options.
- **Finding unexplained state in the working tree (files/edits not created by the current session) gets flagged transparently before acting on it, even mid-otherwise-routine-commit.** The user's response pattern here: acknowledge cleanly ("it was me, commit it") rather than treat the flag as friction. This confirms flagging-before-acting is the right default in this repo, not overcaution to dial back.
- **The user appears to be doing site-build/visualization work in parallel with this conversation** (the donor geography/political-bloc/map files were never produced by anything in this session's own conversation, but the user claimed and committed them). If new unexplained files keep appearing in future sessions, the working assumption should be "probably the user's own parallel work," but still verify content and still flag before committing, per the standing safety practice — don't skip the check just because it resolved benignly last time.

## Work products

| Item | What it is | Where it is | State |
|---|---|---|---|
| `docs/08-decision-log.md` | Full narrative log — read this first | committed | Extensively updated this session; current through the expanded lobbying-to-vote work |
| `pipeline/transform/extract_role_name.py` | Cleans applicant/agent/architect/owner free text, classifies org vs. person | committed | Working |
| `pipeline/transform/build_knowledge_graph.py` | Knowledge graph builder (extended this session) | committed | Working; outputs `data/interim/kg_entities.csv`, `kg_edges.csv` (gitignored, regenerable) |
| `pipeline/transform/normalize_names.py` | Now includes `normalize_postal()` | committed | Working; fixes the postal-space bug |
| `pipeline/analyze/kg_adjudicate_applicant_overlaps.py` | General (not hand-enumerated) adjudication worksheet generator | committed | Working; outputs `audit/kg_corroborated_overlaps_review.csv` (150 rows) |
| `pipeline/analyze/kg_adjudicate_owner_org_overlaps.py` | First-pass narrative notes for the 14 owner-only org matches | committed | Working; outputs `audit/kg_owner_org_overlap_review.csv` |
| `pipeline/analyze/link_lobbying_by_address.py` | Street-address-based lobbying-to-vote linking (new mechanism) | committed | Working; outputs `data/processed/lobbying_address_matches.csv` |
| `pipeline/analyze/classify_lobbying_side.py` | Combines identity+address links, classifies developer vs. opposition side | committed | Working; outputs `data/processed/lobbying_connected_items.csv` |
| `pipeline/analyze/lobbyist_vote_outcomes.py` | Disposition-outcome analysis (rewritten twice this session — see Dead ends) | committed | Working; outputs `data/processed/lobbyist_disposition_outcomes.csv` (107 rows) |
| `data/raw/dev_application_tracking_status/tracking_status.json` | Clerk's Tracking Status text per item, scraped live | **local only, gitignored** | 123 entries (81 from the original identity-only pass + 42 new this session). Not regenerable from a committed script — the scrape itself was done via ad hoc browser page loads, same honesty gap as Session 3 flagged for `all_bgrd_links.json` |
| `docs/07-editorial-legal.md` | Editorial/legal guardrails | committed | **User-edited this session**: removed the cluster-level-presentation preference and the no-reverse-lookup-by-donor-name rule. Not otherwise touched by this session's own pipeline work |
| `data/processed/donor_overlap_table.json`, `donor_geography.json`, `fsa_boundaries.json`, `lakes.json`, `chow_progress_detail.json`, `political_bloc_donors.json`, `political_bloc_fsa.json`, `political_boundaries.json` | Donor geography/political-bloc/map-support data | committed | **User's own work, not produced by this session's pipeline scripts.** No generating script identified in this repo as of this handoff — if regeneration is ever needed, ask the user directly rather than assuming a script exists |
| `handoff-follow-the-ask-2026-08-03-session3.md` | Session 3 handoff | committed | Superseded by this document for anything still-live |

**Important:** if a new chat runs against the same working directory, everything above — including gitignored `data/interim/`, `data/raw/` — will still be there locally. If a new chat runs somewhere else (fresh clone), the gitignored files need their generating scripts rerun where a script exists. `tracking_status.json` specifically has **no generating script** — it was built via interactive browser page loads across this and the prior session; a fresh clone would need this redone by hand (123 items) or the 81+42 split re-derived from this handoff's item lists in `docs/08-decision-log.md`. The donor-geography/political-bloc files similarly have no known generating script in this repo — check with the user before assuming they can be regenerated.

## Verbatim
- Repo: `https://github.com/cmd-siri-bot/follow-the-money`, working dir `C:\Users\iamsi\toronto-2026-election`
- Latest pushed commit on `developer-identity-extraction`: `f141c8e` (verify with `git log origin/developer-identity-extraction..HEAD` — should be empty if push succeeded)
- `main` is still at `c851e28` — this whole branch, all of Sessions 3 and 4's work, is unmerged
- Knowledge graph: registrant↔donor postal-corroborated matches **39 → 148** after the postal-normalization fix; `kg_summary.py`'s same-member overlap metric **8 → 33** (13 of 26 current members)
- Applicant/graph integration: 53 org cross-validations (14 owner-only), 8 person matches (1 corroborated: Emily Reisman)
- Lobbying-to-vote linking: identity match covers 61 of 241 development items; address match adds 46 new, for **107 of 241 (44.4%) total**
- Side classification: **0 of 107 confirmed opposition-side** — verified against both the matched population and all 37 development-related opposition-keyword subject matters in the broader registry, none of whose addresses match any of the 241 Council items
- Disposition: **102 of 107 (95.3%) adopted** overall; **identity-only 22/22 (100%)**; **address-matched 42/46 (91.3%)** — both refusals (`2023.CC5.24`, `2025.MM32.44`) and both other non-adoption outcomes live entirely in the address-matched population
- The two refusals: `2023.CC5.24` (1-3 Concorde Gate/10-12 Concorde Place, 4 registered lobbyists for Fengate Asset Management/Artis Concorde Limited); `2025.MM32.44` (250 Wincott Drive/Richview Square, 5 registered lobbyists for Trinity Development Group)

## Open questions

**For the lobbying-to-vote thread (pick up here if continuing it):**
- A full baseline (adoption rate across all 241 development items, including the ~134 with no established lobbying connection at all) would need scraping Tracking Status for that remaining population — not done this session, explicitly out of scope for "capture lobbying-connected votes."
- Whether the 91.3% vs. 100% adoption-rate gap (address-matched vs. identity-only) survives at a larger sample, or holds up as a real, statistically describable effect rather than an anecdote from 2 refusals — the sample here (46 address-matched items) is still small.
- The `no_recommendation` (`2024.PB15.10`) and `received_no_action` (`2024.PH11.13`) dispositions are new categories this session found but didn't deeply investigate — worth a closer read if this thread continues.

**Still open from Sessions 2–3, untouched this session:**
- Ontario Business Registry pull — explicitly deferred by the user until the core project is complete; do not start without being asked.
- 142 of the 150 rows in `kg_corroborated_overlaps_review.csv` have never had human eyes on them (only the original 8 have prior review) — still the single largest unreviewed population in the project as of this handoff, despite a read-through pass this session (which surfaced patterns but didn't constitute the required human sign-off per docs/02's own standard).
- `docs/07`'s pre-publication checklist (right of reply, third-party advertiser registration check with the City Clerk) — not started. Time-sensitive given the mid-to-late September publication target.
- Phase 4 (site build) — Phase 3's own verdict is still "cannot distinguish" and remains the blocker per docs/06's stated rule, but **the user appears to already be building toward it in parallel** (donor geography/political-bloc/map files this session). Worth explicitly checking with the user whether docs/06's blocking rule is still in force or has been superseded by a decision not captured in this repo's own docs.

## Next action
Two candidate next steps, not mutually exclusive, in the order this session's own momentum suggests:
1. **docs/07's pre-publication checklist** — the most overdue, load-bearing gap, and time-sensitive given the September target.
2. **Read through the 142 unreviewed rows in `kg_corroborated_overlaps_review.csv`** — your own required sign-off before any of those names go further, per this project's own standing rule.

If a new chat picks this up: read `docs/08-decision-log.md` first (as always), then check `git log origin/developer-identity-extraction..HEAD` to confirm the push landed, then ask the user directly about the docs/06 Phase-4-blocking question above before assuming either that the site build is blocked or that it's clear to proceed.
